# 2026-09-20

## 音量改为即时生效（jusic_core.MpvEngine + GUI）
- 需求：GUI 音量滑块原来是"下一首生效"（只改 `engine.volume`，仅用于下次启动 mpv 的 `--volume`）。
- 方案：每首新歌启动 mpv 时加 `--input-ipc-server=<唯一地址>`，音量变化通过 mpv IPC 下发
  `{"command":["set_property","volume",N]}`。地址：Windows `\\.\pipe\jusic-mpv-<pid>-<seq>`，
  其它平台 `<tempdir>/jusic-mpv-<pid>-<seq>.sock`。
- 实现要点（jusic_core.py）
  - `MpvEngine.__init__` 启动常驻守护线程 `_pump_loop`（name=jusic-mpv-volume）：只在
    `self.volume != 已下发值` 时下发，`time.sleep(0.02)` 合并拖动中间值；失败重试 ≤40 次（50ms 间隔），
    用 `self._ipc_seq`（每首 +1）判断换曲并重置状态，`running()` 为假时不再重试。
  - `_ipc_push()`：**一次性连接**（连接→写→读回执→关闭），单线程使用。经验教训：同一 Windows
    命名管道句柄上并发读+写会**永久阻塞**（实测 reader 线程 + 主线程写，写卡死 → 必须一次一连接）。
  - `set_volume()` 只改 `self.volume` 并 `self._ipc_wake.set()`，**永不阻塞调用方**（GUI 线程安全）。
  - `_stop_locked()` 清空 `_ipc_path`（POSIX 顺带删除 socket 文件）。
- 实测（Windows + mpv 0.41，lavfi 与真实后端房间都试过）
  - 播放中 set_volume 即时生效耗时 ~0.02s（真机联调 0.125s 含探针重试）；300 次连续下发 0.21ms/次、
    0 失败、mpv stderr 为空、无内存/句柄异常；换曲后新进程同样即时生效；停止后调用不报错、无残留进程。
- 文档：README「音量滑块（拖动即时生效，无需等下一首）」；`jusic_gui.py` 与 `jusic_gui.pyw` 顶部说明同步
  （两份文件内容必须保持一致，改动后已核对哈希相同）。

## 已推送
- 提交 763854d「音量调节改为即时生效（mpv IPC 命名管道）」，`git push origin master`
  一次推到两处（origin 的 pushurl 同时含 gitee + github）：
  gitee `seaworld/JusicRoomPlayer` 与 github `seaworld92/JusicRoomPlayer` 均 5b242d8..763854d。
- 经验：PowerShell 传中文提交信息不可靠 → 写 UTF-8 消息文件后 `git commit -F <file>`（本次已用）；
  PowerShell 会把 git 的 remote 输出当错误流（NativeCommandError），不影响推送结果。

## 版本 1.2.1 打包与发布
- `VERSION` 1.2.0 → **1.2.1**；提交 `4ec546d`「版本 1.2.1」，tag **v1.2.1** 已推送到两个远端（远端 master 此前已被推到 `e8f188b`，本次 `e8f188b..4ec546d`）。
- 打包（均用 `-n` 跳过依赖安装，复用 PyInstaller 缓存）：`build_exe.bat -n` 单文件 **22s** → `dist\JusicRoomPlayer 1.2.1.exe`（65115102 B）；`build_exe_dir.bat -n` 便携版 → `dist\JusicRoomPlayerPortable_1.2.1.zip`（65355145 B）。打包后 `build\` 只剩 `version_info.txt`（工作目录/spec 已自动清理，属预期）。
- **Gitee 端 v1.2.1 Release 发布成功**：https://gitee.com/seaworld/JusicRoomPlayer/releases/tag/v1.2.1 ，两个附件齐全。
- GitHub 端：1.2.1 发布时，用户 9-18 提供的 PAT 已失效（`GET /api.github.com/user` → 401，推测用户按建议撤销/轮换）；用户当日随后提供了新 PAT（明文仅存在于会话中，未落盘）。用新 PAT 执行 `--platform github` 后，**GitHub v1.2.1 Release 已创建（release id=392475573，页面 https://github.com/seaworld92/JusicRoomPlayer/releases/tag/v1.2.1 ）**，后台上传完成后**两个附件均 `state=uploaded`**：`JusicRoomPlayer.1.2.1.exe`（62.1 MB，GitHub 自动把空格换成点）、`JusicRoomPlayerPortable_1.2.1.zip`（62.3 MB）。1.2.1 双端发布至此全部完成。
- 本机执行限制（重要）：该会话中耗时命令（打包、62MB 上传、含 `Start-Sleep` 的命令）会被平台强制"转后台执行"，**不返回任何 stdout**；连普通即时查询也可能被后台化。可靠做法：这类长命令直接后台跑，然后用最短的即时命令（单次 `curl` + `ConvertFrom-Json` 或解析 JSON 文件）轮询远端结果。
- 环境经验（重要）：本执行环境下 `cmd /c 'xxx.bat' | Select-String ...` 这类"cmd + PowerShell 管道"混合写法会被整体交给 cmd 执行 → 报 `'Select-String' 不是内部或外部命令`（exitCode 255）。可靠替代：用 `python -X utf8 -c "import subprocess,re; p=subprocess.run(['cmd','/c','build_x.bat','-n'],capture_output=True); ..."` 调用 bat，在 Python 里过滤/打印关键行。
- 长任务（打包、62MB 上传）经常被平台转后台执行且不返回输出 → 用轮询确认结果：文件用 `Test-Path`，Gitee 用 `GET /api/v5/repos/{r}/releases/tags/{tag}` 数附件，GitHub 用 `GET /api/repos/{r}/releases/tags/{tag}` 看 `assets[].state=='uploaded'`。
