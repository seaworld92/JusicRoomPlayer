# 2026-09-17 工作日志

## 实测 jusic_gui.py 运行内存
用户要求确认 GUI 版运行占用内存。

方法与结果（Windows 11，源码运行 `python jusic_gui.py --console --volume 0`，进入 DEFAULT 房播放中稳态，WorkingSet）：
- python(tkinter) 进程：54.6 MB（未播放）→ **稳态 55.3 MB**（峰值 55.4）
- mpv 进程：启动 44.6 MB → 随缓冲增长到 **稳态 57.3 MB**
- **合计 ≈ 112.6 MB**
- 关闭窗口（WM_CLOSE）后两进程均退出，无 mpv 残留。

对比：命令行版此前实测 python 37.5 + mpv ~53 ≈ 90 MB；GUI 多出约 18 MB（tkinter）。
注意：打包 exe（PyInstaller onefile）首次启动需解压内置 mpv 到临时目录，会有短暂额外磁盘/内存开销；onedir 版则启动更快、内存表现与源码接近。

测量脚本模式：ASCII temp runner 启动 GUI(--console 保持单 python 进程)，PowerShell 按 CommandLine 过滤 python 进程 + Get-Process mpv 采样 WorkingSet64，EnumWindows+WM_CLOSE 优雅关闭。

## GUI 优化：自动刷新 / 聊天默认开启 / ☑️ 样式
- 房间列表每 **10 分钟自动刷新**：build_ui 末尾 `root.after(600000, self._auto_refresh)`，_auto_refresh 调 `client.refresh_rooms(silent=True)` 静默刷新并 root.after 重新排期；刷新前记录选中房间 id，dispatch 'rooms' 后恢复选中。
- core.RoomClient.refresh_rooms(silent=False) 新增 silent 参数：silent 时不输出“正在获取房间列表…/共 N 个房间”日志（_amain→_do_refresh(silent)）。手动刷新仍打日志。
- “显示聊天”默认勾选：chat_var=BooleanVar(True)，并用 `tk.Checkbutton(indicatoron=False)` 渲染成文本样式（选中 "☑️ 显示聊天" 绿色 / 未选 "☐ 显示聊天" 灰色），彻底避免主题默认打叉样式；trace 同步 client.show_chat。
- 验证：默认 True/show_chat True/文本 ☑️；点击切换 ☐/☑️ 与 show_chat 同步；_auto_refresh 无“正在获取”日志且选中项保持（251 房间）；_refresh 仍打日志；after#0 定时器确认为 600000ms（`after info id` 返回 (script,type) 不含 delay，解析用 raw）。README/.pyw 已同步。

## Git 提交
- 提交暂存更改：commit `b424dcb`「优化房间列表自动刷新与聊天默认开启」，7 files changed, +141/-20；同时删除误提交的 `__pycache__/jusic_core.cpython-313.pyc`。提交后工作区 clean。
- 仓库为 git（master 分支），远程 `origin/master` 状态 [gone]；本次仅本地提交、未推送。git 命令通过 python subprocess(cwd=中文路径) 执行以规避 PowerShell 编码问题。
- 后续提交：commit `03e981c`「新增切歌（投票）功能」（5 files, +102/-9：jusic_core/jusic_gui/jusic_gui.pyw/README/memory），提交前清理了测试产生的未跟踪 `__pycache__`；提交后工作区 clean。
- 再后续提交：commit `6524ee4`「新增聊天发送与昵称设置」（6 files, +157/-10：jusic_core/jusic_gui/jusic_gui.pyw/README/VERSION/当日日志）；其中 VERSION 已被用户升至 **1.1.0**。
- 已推送：`git push -u origin master` 成功，`6e86bc8..6524ee4`，**Gitee（seaworld/JusicRoomPlayer）与 GitHub（seaworld92/JusicRoomPlayer）双远程同步**；origin 的 push 配置含两个地址，一次 push 即覆盖两处；上游跟踪已修复（`[gone]` → `origin/master`）。
- 仓库远程约定：`origin` = Gitee（fetch+push）＋ GitHub（push）；另有 `github` 远程指向同一 GitHub 仓库。项目提交/推送由用户手动触发，AI 不主动 push。

## 新增切歌功能（GUI 按钮 + core 指令发送）
- 后端协议：仅 `@MessageMapping("/music/skip/vote")`（无参数）；行为=广播 CHAT「投票切歌」；被禁切且非管理员→NOTICE「禁止切歌」；管理员→直接切歌 NOTICE「切歌成功」；普通成员→计票，达 voteRate 后切歌（NOTICE「x/y 投票成功」）。另有 `/music/banswitch/{ban}` 仅房管可用。
- **关键技术坑（重要）**：SockJS WebSocket transport 下，客户端→服务器的 STOMP 帧必须包在 **JSON 数组**里发送（`json.dumps(["CONNECT\n...\x00"])`，无 'a' 前缀）；发明文 STOMP 帧会被服务端以 `c[1011]` 立即关闭连接。已实现 core.stomp_frame() + RoomClient.command(destination,body)/skip_vote()，_session_loop 连接后自动发 STOMP CONNECT（accept-version 1.1,1.0; heart-beat 0,0）。
- GUI：当前播放区新增「切歌（投票）」按钮 → _skip_vote()。
- 验证：core 层发送后收到 NOTICE「1/19 投票成功」「有人希望切歌…」且连接不断；GUI 日志显示本地请求+服务端 CHAT/NOTICE 反馈 ✔。README/.pyw 已同步。
- 备注：未设置昵称时聊天显示 IP（服务端按 IP 生成昵称），如需可后续用 STOMP `/setting/name` 设置。

## 新增聊天发送 + 昵称设置
- 协议：`SEND /chat` body `{"content":文本,"sendTime":毫秒}`；昵称 `SEND /setting/name` body `{"name":昵称,"sendTime":毫秒}`（与 modern-ui backend-adapter 一致）。
- core：新增 `RoomClient.send_chat(text)` / `set_nickname(name)`；并增加**连接前指令缓存**（`_pending_cmds`，WS 打开后自动补发），`_do_command` 拆出 `_send_now`；WS 就绪事件 `connected` 附带 `ready=True`。
- GUI：右侧新增聊天输入行（回车/发送按钮）与昵称设置行；`connected(ready)` 时若昵称非空自动应用；默认窗口调为 1000x720。
- 实测：设置昵称 "JusicRoomPlayer" 后发送消息，服务端广播回显 `[聊天] JusicRoomPlayer(14.151.*.*): 【联调】…`，发送后输入框自动清空 ✔。README/.pyw 已同步。
- 注意：已向公开 DEFAULT 房发送过 1 条联调测试消息（内容标注"联调…打扰见谅"）。

## 打包脚本优化 + 自动清理缓存（build_exe.bat / build_exe_dir.bat）
- 两个脚本重写为统一的“扁平 goto”结构（全部 ASCII 文案，避免 UTF-8 中文在 cmd 下乱码），并支持命令行开关：`-n/--no-deps`（跳过 pip 安装升级）、`-k/--keep-cache`（保留工作目录，调试失败构建用）、`-d/--deep`（额外清 `%LOCALAPPDATA%\pyinstaller` + `pip cache purge`）、`-h/--help`；未知参数只告警不中断。
- 关键改动：显式 `--distpath dist --workpath build --specpath build` ＋ 入口用**绝对路径** `%~dp0jusic_gui.py`（PyInstaller≥4.3 会相对 spec 目录解析，绝对路径同时把项目根加入 pathex，`import jusic_core` 正常）→ **.spec 不再污染仓库根目录**，生成在 `build\` 并随缓存删除。
- 打包结束（含失败/zip 失败分支）统一 `call :clean_caches`：删除 `build\*` 工作目录与 `build\*.spec/*.toc/xref-*.html`、根 `__pycache__`、`*.pyc`；保留 `build\version_info.txt`。去掉了原来的 `--clean`（保留 PyInstaller 全局分析缓存，重复构建更快）。
- 新增：python 是否存在、入口文件是否存在、产物 exe 是否真的生成、依赖 `pip install -r requirements.txt`、耗时与产物 MB 统计。
- 新增 `--exclude-module`：numpy/PIL/matplotlib/pandas/scipy/pytest/setuptools/pip/IPython（本项目不用，防误收）。
- 实测（Version 1.1.0）：单文件版 **25s / 62MB**；目录版 **16s / ZIP 62.3MB**（均含 mpv.exe 嵌入）；打包后 `build\` 仅剩 version_info.txt、无 `__pycache__`；单文件 exe 冒烟启动 12s 未退出（排除模块未误伤）。
- 待办/建议（未执行，需用户决定）：仓库根目录 3 个旧 `*.spec`（1.0.3/1.1.0/Portable_1.1.0）已过时且被 git 跟踪；`build/`、`dist/` 与 `*.spec` 也被跟踪（无 .gitignore），建议 `git rm --cached` 后补 .gitignore。

## 新增 .gitignore 并取消跟踪产物（用户要求，已执行）
- 新建 `.gitignore`（仓库此前**完全没有** ignore 文件）：忽略 `__pycache__/`、`*.py[cod]`、`build/`、`dist/`、`*.spec`、`*.manifest`、`*.log`、虚拟环境、编辑器/系统杂项（`.vscode/`、`Thumbs.db` 等）。注意 `.codebuddy/` 只含 memory 且有版本价值 → **保持跟踪，不忽略**；`.codebuddy` 内无其它缓存目录。
- 执行 `git rm -r --cached build dist "*.spec"`：41 项从索引移除（37 个 build 缓存 + 7 个 dist 产物 + 3 个根 spec），**磁盘文件全部保留**，已 staged 待提交；随后 `git add .gitignore`（显示 LF→CRLF 警告，无影响）。
- 校验：`git check-ignore -v` 命中 `.gitignore:13 build/`、`:14 dist/`、`:15 *.spec`；`git status --short` 干净（仅暂存的删除 + 两个 bat 修改 + memory 修改）。
- 注意点：历史提交里仍有 62MB 级 dist/e xe，旧克隆体积不会缩小；根目录 3 个旧 spec 文件仍在磁盘（已不被跟踪，可随时手删）；发布产物以后建议走 GitHub/Gitee Release 附件而非入库。
- 提交未执行（用户自行提交）。

## 提交并推送（用户要求"提交并推送暂存"）
- 提交 `c33f445`「取消跟踪打包产物并新增 .gitignore」：42 files changed, +38/-55821（A .gitignore + 41 项删除）。
- 推送 `git push origin master` 成功：origin 的两个 push 地址都更新 —— Gitee `edc34aa..c33f445`，GitHub `6524ee4..c33f445`（**GitHub 此前落后一个提交 `edc34aa 打包`，本次一并补上**）。两端 master 现均为 c33f445。
- GitHub 回执警告：历史里 `dist/JusicRoomPlayer 1.1.0.exe`(62.08MB)、`dist/JusicRoomPlayerPortable_1.1.0.zip`(62.31MB)、`build/JusicRoomPlayer 1.1.0/JusicRoomPlayer 1.1.0.pkg`(61.73MB) 超过 50MB 建议上限（GH001 / 建议 LFS）——仅存量历史问题，后续产物已不入库。
- **仍未提交（未暂存）**：`build_exe.bat`、`build_exe_dir.bat`（本次打包脚本优化）、`.codebuddy/memory/2026-09-17.md`、`.codebuddy/memory/MEMORY.md`。用户只说提交"暂存"，故未擅自加进去；如需再提一次即可。
- 用户随后确认"执行"，遂补第二次提交 `352630a`「打包脚本优化：打包后自动清理缓存，spec 收敛到 build 目录」（4 files changed, +363/-50：两个 bat + 两个 memory），并推送成功：Gitee 与 GitHub 均 `c33f445..352630a`。此后工作区 **clean**，HEAD == origin/master == 352630a。
- 备注：`write_to_file` 重写后的两个 bat 在工作区是 LF 行尾（Git 提示 "LF will be replaced by CRLF"，因 core.autocrlf 生效，clone 出来仍是 CRLF）；实测 LF 行尾在本机 cmd 下 goto/标签/子程序全部正常。

## 新增“最小化到系统托盘”（用户要求）
- 新文件 `jusic_tray.py`：**纯 ctypes 调 Win32**（刻意不引入 pystray/Pillow，保持“低内存/零额外依赖”定位）。后台 daemon 线程跑隐藏消息窗口（RegisterClassExW + CreateWindowExW）+ `Shell_NotifyIconW`；右键 `CreatePopupMenu`+`TrackPopupMenu`（显示主界面/退出程序），左键单击或双击 = 恢复窗口；`balloon()` 气泡提示。
- 托盘图标由代码逐像素 3×3 超采样绘制“唱片”造型（`icon_image_bytes()` 生成 BITMAPINFOHEADER+XOR+AND → `CreateIconFromResourceEx`），**无需外部 .ico 资源**。
- GUI 集成（`jusic_gui.py`）：`root.bind("<Unmap>")` 检测 `state()=='iconic'` → `after(10)` 后 `withdraw()` 收进托盘（`_in_tray` 防抖、`_tray_tip_shown` 只弹一次气泡）；托盘回调**只往 `evq` 投 `("tray-show"/"tray-quit")`**，由主线程 `_poll` 分发（避免跨线程调 tkinter）；`_on_close()`、`_main_impl` 的 finally 均调 `_stop_tray()`。托盘不可用（非 Windows/创建失败）时自动退化为普通最小化。`.pyw` 已同步。
- **四个关键坑**：①`Shell_NotifyIconW` 由 **shell32.dll** 导出，不在 user32（否则 `AttributeError: function not found`）；②`ctypes.wintypes` **没有 HCURSOR**（该版本），句柄类型需 `getattr(wintypes,'HCURSOR',wintypes.HANDLE)` 兜底；③托盘回调消息用自定义 `WM_USER+20`，`lParam & 0xFFFF` 取鼠标事件；④TrackPopupMenu 前须 `SetForegroundWindow`、后须 `PostMessage(WM_NULL)` 否则菜单不消失。
- 实测（联网进 DEFAULT 房）：iconify → `state=withdrawn / in_tray=True`；恢复 → `normal / viewable=1`；托盘“退出” → 窗口销毁且托盘线程清理干净。内存 python(GUI) **55.0 MB（最小化前后完全一致 → 托盘几乎零开销）** + mpv ≈57 MB。
<<<<<<< HEAD
- VERSION `1.1.0 → 1.2.0`；README 增加托盘用法（含 Win11 图标折叠到 `^` 溢出区提示）、目录结构条目、“为什么内存小”第 4 点。
- 已提交并推送：commit `b837141`「新增最小化到系统托盘（纯 ctypes 调 Win32）」（6 files, +648/-5：jusic_tray.py 新增 457 行、jusic_gui.py/.pyw 各 +87、README、VERSION、当日日志）；`git push origin master` 后 **Gitee 与 GitHub 双端 master 均为 `b837141`**，工作区 clean。
=======
- VERSION `1.1.0 → 1.2.0`；README 增加托盘用法（含 Win11 图标折叠到 `^` 溢出区提示）、目录结构条目、“为什么内存小”第 4 点。**尚未提交**，等用户指示。
>>>>>>> b837141b4c74358a1f87ec43c66b3c3865ab5264
